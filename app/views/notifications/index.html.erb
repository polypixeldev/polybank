<% title "Notifications" %>

<h2 class="font-bold text-2xl mb-3">Notifications</h2>

<div class="flex flex-col gap-3">
  <%= render partial: "notifications/notification", collection: @notifications %>
</div>
