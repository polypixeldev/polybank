<%# locals: (budget:) %>

<%= link_to budget_path(budget), class: "grid grid-cols-15 px-4 py-3 bg-slate-800 rounded-md items-center" do %>
  <p><%= budget.name %></p>
  <p><%= budget.period %></p>
<% end %>
