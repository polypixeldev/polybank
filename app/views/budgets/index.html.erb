<% title "Budgets | Polybank" %>

<div class="flex gap-3 mb-3">
  <h2 class="font-bold text-2xl">Budget Search</h2>

  <%= link_to "New", new_budget_path, class: "inline-btn" %>
</div>

<%= render partial: "budgets/budget", collection: @budgets %>
