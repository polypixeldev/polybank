<%= turbo_frame_tag "#{@transaction.id}_edit_memo" do %>
  <%= form_with model: @transaction, method: :put do |form| %>
    <%= form.text_field :memo, class: "bg-white rounded-md px-2 py-1 mt-1 mb-3 text-black w-full" %>
  <% end %>
<% end %>
